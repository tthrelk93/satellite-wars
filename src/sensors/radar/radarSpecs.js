export const DEFAULT_GROUND_DOPPLER_SPECS = Object.freeze({
    rangeMaxKm: 230,
    ppiSizePx: 512,
    elevationDeg: 0.5,
    gateLengthKm: 1.0,
    beamWidthDeg: 1.0,
    vNyquistMps: 25
});

export const RADAR_PRODUCT = Object.freeze({
    DBZ: 'dbz',
    VR: 'vr'
});
